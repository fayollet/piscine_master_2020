On dockerhub create a repository then


```
docker build -t eliotctl/step05:latest .
docker push eliotctl/step05:latest
```

`<eliotctl>` is the current **username**
`<step05>` is the repository **name**
